/**
 * Optional standard citation ids used by the work-episode ontology.
 *
 * These are opaque string tags — not a full IICRC registry. The mitigation
 * estimator that used to own this vocabulary has been removed from the
 * hosted Work Verification / Field Capture product.
 */
export type CitationId = string;
