# Shared Docker context (repo root). Do not exclude frontend, verifier,
# fieldcapture, or backend — different Dockerfiles COPY those trees.
.git
website
apps
agent
docs
**/node_modules
**/dist
**/.env
**/.env.*
!.env.example
