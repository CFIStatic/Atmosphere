# Keep Railway's root Docker build small (backend image).
# frontend/, verifier/, and fieldcapture/ stay in the context so
# `docker build -f frontend/Dockerfile .` can copy them.
.git
website
apps
agent
docs
**/node_modules
**/dist
**/.env
**/.env.*
!.env.example
