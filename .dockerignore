# Shared repo-root Docker context (backend, office, field, website, internal).
# Do not exclude frontend/, verifier/, fieldcapture/, website/, or internal/ — those
# Dockerfiles COPY them from this context.
.git
apps
agent
docs
**/node_modules
**/dist
**/.env
**/.env.*
!.env.example
