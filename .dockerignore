# Shared repo-root Docker context (backend, office app, and website images).
# Do not exclude frontend/, verifier/, fieldcapture/, or website/ — those
# Dockerfiles COPY them from this context.
.git
apps
agent
docs
**/node_modules
**/dist
**/.env
**/.env.*
!.env.example
