{
  "name": "Atmosphere",
  "install": "cd backend && npm install && cd ../frontend && npm install",
  "start": "bash .cursor/ensure-api.sh --background",
  "ports": [
    { "name": "website", "port": 5173 },
    { "name": "app", "port": 5174 },
    { "name": "api", "port": 4000 }
  ],
  "terminals": [
    {
      "name": "backend",
      "command": "bash .cursor/ensure-api.sh --foreground",
      "description": "Express API on :4000 (idempotent — follows logs if start already launched it)"
    },
    {
      "name": "website",
      "command": "cd frontend && npx vite --config website.vite.config.ts --host",
      "description": "Corporate marketing site on :5173 (open this first)"
    },
    {
      "name": "app",
      "command": "cd frontend && npm run dev -- --host",
      "description": "React product app on :5174"
    }
  ]
}
